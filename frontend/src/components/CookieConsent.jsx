// frontend/src/components/CookieConsent.jsx
// Blue banner DISABLED on purpose. We rely on LeadConnector's white banner instead.

import React from "react";

/**
 * Intentionally renders nothing. This keeps your App.js imports intact
 * but removes our cookie UI completely.
 */
const CookieConsent = () => null;

export default CookieConsent;