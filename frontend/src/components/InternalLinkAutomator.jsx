import React from 'react';
// import { Link } from 'react-router-dom';
// INTERNAL_LINK_MAP removed - not defined in mock.js

const InternalLinkAutomator = ({ text }) => {
  // Component disabled - INTERNAL_LINK_MAP not available
  // TODO: Re-implement when INTERNAL_LINK_MAP is properly defined
  return text || null;
};

export default InternalLinkAutomator;